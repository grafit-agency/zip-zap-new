export type Player = {
  id: string;
  full_name: string;
  attempts: number;
  high_score: number;
  created_at: string;
};

export type LeaderboardRow = {
  full_name: string;
  attempts: number;
  high_score: number;
};


